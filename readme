# 1. create user in UI
# 3. Yeelight should import lights automatically
# 4. Install Gateway3 and import main gateway (fill ip, token, key) and it will import all devices
# 5. Install xiaomi-miot and import main gateway (fill ip, token, key) and it will import all devices
# 6. Install MQTT (mosquitto.mosquitto 1883 homeassistant FLS3GD8Qc8xJWp) (needed for Qinqpong only)
# 7. Qinqpong pro add manually (livingroom_air 582D34006AAA). Need to be sure that device send data in mqtt using mqtt explorer.
# 8. (If lock not work then maybe need to install pyzk inside container because zk_teco very old)
# 9. Install ONVIF add all cameras manually
# 10. Generic camera add elevators camera manually and change name additionaly (rtsp://192.168.2.111:554/live.sdp basic)

# for testing config
C:\Users\OTHER\kubectl cp configuration.yaml homeassistant/amur-564b8b64d6-lbczt:/config