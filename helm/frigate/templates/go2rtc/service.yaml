apiVersion: v1
kind: Service
metadata:
  name: go2rtc
  namespace: {{ .Release.Namespace }}
  annotations:
    prometheus.io/port: "8000"
    prometheus.io/scrape: "false"
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 1984
    targetPort: 1984
    protocol: TCP
  selector:
    app: go2rtc