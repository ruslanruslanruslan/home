apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Namespace }}-deployment
  namespace: {{ .Release.Namespace }}
spec:
  storageClassName: directpv-slow-delete
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
