apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Namespace }}-deployment
  namespace: {{ .Release.Namespace }}
spec:
  storageClassName: directpv-sata-ssd-raw-delete
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
